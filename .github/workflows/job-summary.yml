name: Job summary
on: push
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - run: |
          {
            echo "## Example title :rocket:"
            echo "- first line"
            echo "- second line"
          } >> "${GITHUB_STEP_SUMMARY}"
